import logo from '@/assets/logo.svg';
import RoundIconButton from '@/common/components/buttons/round-icon-button';

const Week3Example = () => {
  return <RoundIconButton icon={logo} isSpecial size={200} />;
};

export default Week3Example;